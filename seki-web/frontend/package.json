{
  "name": "seki-frontend",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs --watch",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "classnames": "^2.5.1",
    "preact": "10.x"
  },
  "devDependencies": {
    "esbuild": "^0.x",
    "typescript": "5.x"
  }
}
